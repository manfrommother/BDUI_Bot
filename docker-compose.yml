services:
  bot:
    build: .
    container_name: telegram-daily-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - STATE_FILE=/data/state.json
    volumes:
      - bot_state:/data
    command: ["python", "bot.py"]

volumes:
  bot_state:
